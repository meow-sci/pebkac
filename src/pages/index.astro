---
import { Pointer } from "lucide-react";
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <style slot="head">
    body {
      padding: 1rem;
      background-color: #222222;

      color: #00ff00;
    }
    h1 {
      font-family: "Oxanium Variable", system-ui;
      font-size: 3rem;
      font-weight: 700;
      margin: 0;
      padding: 0;
    }
    h6 {
      font-family: "Oxanium Variable", system-ui;
      font-size: 0.875rem;
      font-weight: 300;
      margin: 0;
      padding: 0;
    }
    #main {
      display: grid;
      grid-auto-flow: row;
      align-items: center;
      justify-items: center;
    }
    #brought-to-you-by {
      margin-top: 4rem;
      display: grid;
      grid-auto-flow: row;
      align-items: center;
      justify-items: center;
      row-gap: 0.25rem;
    }

    #byline {
      margin-bottom: 0.5rem;
      font-size: 0.875rem;
      font-weight: 300;
    }

    #and {
      font-size: 1.5rem;
      font-weight: 200;
    }

    #briarthrain {
      font-family: "Asimovian", system-ui;
      font-size: 2rem;
      font-weight: 700;
    }
    #tomservo {
      font-family: "Megrim", system-ui;
      font-size: 2rem;
      font-weight: 200;
    }
    /* Gold glow animation for byline names (subtle) */
    @keyframes goldGlow {
      0% {
        text-shadow:
          0 0 4px rgba(255, 215, 0, 0.1),
          0 0 8px rgba(255, 215, 0, 0.08);
      }
      50% {
        text-shadow:
          0 0 10px rgba(255, 217, 0, 0.5),
          0 0 12px rgba(255, 165, 0, 0.25);
      }
      100% {
        text-shadow:
          0 0 4px rgba(255, 215, 0, 0.1),
          0 0 8px rgba(255, 215, 0, 0.08);
      }
    }

    #briarthrain,
    #tomservo {
      /* keep original green text color from body #00ff00; only animate gold glow */
      animation: goldGlow 4000ms ease-in-out infinite; /* shorter full cycle */
      /* GPU hint: animate the transform/text-shadow only */
      will-change: transform, text-shadow;
      /* base gold glow only (subtle) */
      text-shadow:
        0 0 4px rgba(255, 215, 0, 0.2),
        0 0 8px rgba(255, 215, 0, 0.08);
    }
    .v-spacer-2x {
      height: 1.5rem;
    }
    .v-spacer-1x {
      height: 0.5rem;
    }

    .thelink {
      position: relative;
    }

    .pointer-left-1 {
      position: absolute;
      right: calc(100% + 0.5rem);
      top: -0.5rem;
      width: 1.25rem;
      height: 1.25rem;
      transform-origin: center;
      pointer-events: none;

      /* keep arrow orientation only */
      transform: rotate(120deg);
      will-change: right;

      /* animate 1rem away from where it points, and back */
      animation: pointer-left-1-wiggle 1s ease-in-out infinite;
      animation-delay: 0s;
      animation-fill-mode: both;
      animation-direction: alternate;
    }

    .pointer-left-2 {
      position: absolute;
      right: calc(100% + 0rem);
      top: 1.75rem;
      width: 1.25rem;
      height: 1.25rem;
      transform-origin: center;
      pointer-events: none;

      transform: rotate(50deg);
      will-change: right;
      animation: pointer-left-2-wiggle 1s ease-in-out infinite;
      animation-delay: 0.18s;
      animation-fill-mode: both;
      animation-direction: alternate;
    }

    .pointer-right-1 {
      position: absolute;
      left: calc(100% + 0.5rem);
      top: -0.75rem;
      width: 1.25rem;
      height: 1.25rem;
      transform-origin: center;
      transition: transform 160ms ease;

      transform: rotate(-120deg);
      will-change: left;
      animation: pointer-right-1-wiggle 1s ease-in-out infinite;
      animation-delay: 0.1s;
      animation-fill-mode: both;
      animation-direction: alternate;
    }

    .pointer-right-2 {
      position: absolute;
      left: calc(100% + 0.5rem);
      top: 1rem;
      width: 1.25rem;
      height: 1.25rem;
      transform-origin: center;
      transition: transform 160ms ease;

      transform: rotate(-60deg);
      will-change: left;
      animation: pointer-right-2-wiggle 1s ease-in-out infinite;
      animation-delay: 0.24s;
      animation-fill-mode: both;
      animation-direction: alternate;
    }

    /* Keyframes animate the position property so each pointer
       moves along a straight line in/out from the link. */
    @keyframes pointer-left-1-wiggle {
      0%   { right: calc(100% + 0.5rem); }
      50%  { right: calc(100% + 1.5rem); }
      100% { right: calc(100% + 0.5rem); }
    }
    @keyframes pointer-left-2-wiggle {
      0%   { right: calc(100% + 0rem); }
      50%  { right: calc(100% + 1rem); }
      100% { right: calc(100% + 0rem); }
    }

    @keyframes pointer-right-1-wiggle {
      0%   { left: calc(100% + 0.5rem); }
      50%  { left: calc(100% + 1.5rem); }
      100% { left: calc(100% + 0.5rem); }
    }
    @keyframes pointer-right-2-wiggle {
      0%   { left: calc(100% + 0.5rem); }
      50%  { left: calc(100% + 1.5rem); }
      100% { left: calc(100% + 0.5rem); }
    }
  </style>

  <div id="main">
    <h1>PEBKAC</h1>
    <h6 class="acronym">
      Problem Exists Between Kittens and Chair
    </h6>

    <p>
      <a class="thelink" href="/pebkac/builder/">
      <Pointer className="pointer-left-1" />
      <Pointer className="pointer-left-2" />
        Pick a celestial, any celestial!
      <Pointer className="pointer-right-1" />
      <Pointer className="pointer-right-2" />
      </a>
    </p>
    <div class="v-spacer-2x"></div>
    <h6>game data baked from KSA v2025.11.6.2939</h6>
    <div class="v-spacer-2x"></div>
    <h6>
      The orbits are currently taken using the J2000 reference Epoch / time
      (mostly)
    </h6>
    <div class="v-spacer-2x"></div>

    <h6>
      ---------------------------------------------------------------------------------------------
    </h6>
    <div class="v-spacer-2x"></div>
    <h6>
      <u><b>WIP</b></u>: Default inclinations / orbits need revision with better
      averaged data and over a larger time scale.
    </h6>
    <div class="v-spacer-2x"></div>
    <h6>
      <u><b>TBD</b></u>: Will show a custom script which can be used to generate
      one's own orbit data (For any object in the NASA Horizons system)
    </h6>
    <h6>
      on a selectable Epoch / time, which can then get uploaded to the site!
    </h6>
  </div>

  <div id="brought-to-you-by">
    <div id="byline">brought to you by</div>
    <div id="briarthrain">Briar Thrain</div>
    <div id="and">&amp;</div>
    <div id="tomservo">tomservo</div>
  </div>
</BaseLayout>
